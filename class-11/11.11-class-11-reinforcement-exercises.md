---
layout: default
title: "11.11 Class 11 Reinforcement Exercises"
published: true
---

# 11.11 Class 11 Reinforcement Exercises (Grant Application Validator)

These reinforcement exercises build on top of the projects from the tutorials: a **Grant Application** form with a mini form rendering and validation framework.

You will practice Chapter 12 skills:

- `filter_input()` sanitization filters (`FILTER_SANITIZE_EMAIL`, `FILTER_SANITIZE_URL`, etc.)
- server-side validation rules (required, allowed values, ranges, formats)
- sticky forms and field-level error messages
- HTML5 validation attributes for UX (`required`, `min`, `max`, `minlength`, `maxlength`, `pattern`)
- PRG (Post-Redirect-Get) with a 303 redirect after successful POST
- saving only validated submissions to a `.json` file
- output escaping with `htmlspecialchars()` / `h()` (XSS prevention)

## Run Locally

From the repository root:

```bash
php -S localhost:8080 -t public public/router.php
```

Then visit:

- `http://localhost:8080/`
- `http://localhost:8080/grant`

## Data Storage

Submissions are stored in:

- `app/storage/data.json`

Shape:

- `applications` array

## Exercise 11-1: New Fields in Spec

Edit:

- `app/lib/fields.php`

Tasks:

- Add a new field to the spec for each of these:
  - `projectDate`
    - *Label*: Anticipated project initiation date
    - *Type*: date
    - *Required*: yes
    - *Validation*: add a `date` rule to the `rules` array with a value of `true` to validate that the input is a valid date format and add a `min` rule with a value of today’s date to ensure the date is not in the past (use `date('Y-m-d')` in PHP)
    - *HTML5 UX*: add `min` attribute to the `html` array set to today’s date (like above, use `date('Y-m-d')` in PHP)
  - `phoneNumber`
    - *Label*: Contact phone number
    - *Type*: tel
    - *Required*: no
    - *Validation*: add a `pattern` rule for a valid phone number format (use a regex pattern, such as `\d{3}-\d{3}-\d{4}` for US numbers)
    - *HTML5 UX*: Add a `pattern` attribute to the `html` array to help users enter a valid phone number format (e.g. `\d{3}-\d{3}-\d{4}` for US numbers)
    - *Placeholder*: Add a `placeholder` attribute to the `html` array to show an example format that matches your pattern (e.g. `123-456-7890`)

Notes:

- If you test these before completing the rest of the exercises, you will see that the new fields are rendered in the form, but they do not yet have server-side validation.
- The field rendering logic in `app/lib/render.php` is designed to automatically handle new fields, input field types, and HTML attributes added to the spec, so you should not need to modify the rendering code to see the new fields appear on the form.

## Exercise 11-2: New Validation Rules

Edit:

- `app/lib/validate.php`

Tasks:

- Update the `validateValues()` function to support the new validation rules needed for the `projectDate` and `phoneNumber` fields:
  - For the `date` rule, check if the value is a valid date format (you can use `strtotime()` to attempt to parse the date and check if it returns a valid timestamp - it returns `false` if the date is invalid or the number of seconds since the Unix Epoch otherwise).
  - Update the current `min` and `max` rules so that they only apply to fields with a type of `number`.
  - Add new `min` and `max` rules that apply only to fields with a type of `date`, using `strtotime()` to compare the input date against the specified minimum and maximum dates - they should be inclusive (`<=` and `>=`) and should be two separate rules. Based on what you have learned so far, I would use `strtotime()` to convert both the input date and the min/max dates to timestamps for comparison, but there are other approaches.
  - For the `pattern` rule, use `preg_match()` to validate that the input matches the specified regex pattern. We're using this for phone numbers right now, but it can be used for other validation purposes as well.

## Exercise 11-3: Sanitize the New Fields

Edit One or Both of These Files:

- `app/lib/input.php`
- `app/lib/fields.php`

Tasks:

- Apply appropriate sanitization filters to the new fields in the spec so that they are sanitized when retrieved in `readValues()`. For example:
  - For `projectDate`, you could indicate in the spec to sanitize using `FILTER_SANITIZE_NUMBER_INT` since it's a date field and we can assume that the browser will only send numeric digits and hyphens, which are allowed by the filter (the plus sign is allowed as well, but we can leave it up to validation to handle that).
  - For `phoneNumber`, you might also use `FILTER_SANITIZE_NUMBER_INT` if you are using the US phone number format described above, but if you choose to allow other formats, you might end up wanting to create type-specific sanitization logic in the `readValue()` function.

**Note**: The autograder is looking for those specific filters for each field, so if you choose to implement the sanitization in `readValue()` instead of in the spec, don't worry about the autograder not recognizing that you have implemented sanitization for those fields. The key is that the new fields should be sanitized when retrieved in `readValues()`, whether that's done by specifying the filter in the spec or by implementing type-specific sanitization logic in `readValue()`.

## Exercise 11-4: Save the New Fields to JSON

Edit:

- `app/lib/functions.php`

Task:

- Update the `buildApplicationRecord()` function to include the new fields in the record that gets saved to JSON.

## Exercise 11-5: Application Viewer

Edit:

- `app/views/pages/grant-viewer.php`

Tasks:

- Create a new page that reads the JSON file and renders a list of submitted applications.
- List just a few key fields (e.g. applicant name, project summary/category, and project date) in the main list and then link to a detail page for each application that shows all the fields.
- Use query parameters to indicate which application to show on the detail page (e.g. `/viewer?id=ga_123456`), and make sure to handle cases where the specified application does not exist.
- Include a link back to the list from the detail page.
- You can choose how to format the output, but the detail should include all fields for each application and any fields shown on either page should be properly escaped to prevent XSS.

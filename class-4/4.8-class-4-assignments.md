# 4.8 Class 4 Assignments

## Exercise 4-1: Basic `if...else`

In this exercise, you will use `if...else` to choose a shipping cost.

1. Open `class-4/starter-files/4-1-basic-if-else.php`.
2. Use `if...else` to set `$shipping` based on `$subtotal`.
3. Run the script and confirm the output matches the expected output in the
  file.

## Exercise 4-2: Logical Operators

In this exercise, you will combine conditions with logical operators.

1. Open `class-4/starter-files/4-2-logical-operators.php`.
2. Set `$eligible` using `&&` and `||` as described in the comments.
3. Run the script and confirm the output matches the expected output in the
  file.

## Exercise 4-3: `switch` or `match`

In this exercise, you will map a value to a message.

1. Open `class-4/starter-files/4-3-switch.php`.
2. Use either `switch` or `match` to set `$message` based on `$status`.
3. Run the script and confirm the output matches the expected output in the
  file.

## Exercise 4-4: Ternary and Null Coalescing

In this exercise, you will use the ternary operator and `??` for defaults.

1. Open `class-4/starter-files/4-4-ternary-null-coalescing.php`.
2. Set `$label` with a ternary expression.
3. Set `$displayName` with null coalescing.
4. Run the script and confirm the output matches the expected output in the
  file.

## Exercise 4-5: Regex Password Check

In this exercise, you will use a provided regular expression to check
password complexity and output a message.

1. Open `class-4/starter-files/4-5-password-regex.php`.
2. Use `preg_match` with the provided pattern to set `$isStrong`.
3. Set `$message` based on `$isStrong`.
4. Run the script and confirm the output matches the expected output in the
  file.

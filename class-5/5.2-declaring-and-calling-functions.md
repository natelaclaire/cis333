---
layout: default
title: "5.2 Declaring and Calling Functions"
published: true
---

# 5.2 Declaring and Calling Functions

In this video, we cover the basics of custom functions: declaring them,
calling them, passing parameters, and returning values.

## Declaring a Function

Use the `function` keyword. A function can have parameters and a return type.

```php
<?php
function greet(string $name): string
{
    return 'Hello, ' . $name . '!';
}
?>
```

## Calling a Function

Call the function by name with parentheses. The return value can be stored,
passed to another function, or printed directly.

```php
<?php
$message = greet('Ada');
print $message . PHP_EOL;
?>
```

## Parameters and Return Types

Type declarations make your intent clear and help PHP catch mistakes.

```php
<?php
function add(int $a, int $b): int
{
    return $a + $b;
}

$sum = add(5, 3);
print 'sum: ' . $sum . PHP_EOL;
?>
```

In the next videos, we will go deeper on return behavior, optional
parameters, and pass-by-reference.

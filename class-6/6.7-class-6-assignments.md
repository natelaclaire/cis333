---
layout: default
title: "6.7 Class 6 Assignments"
published: true
---

# 6.7 Class 6 Assignments

## Exercise 6-1: `while` Loop Sum

In this exercise, you will use a `while` loop to compute a sum using a
counter variable.

1. Open `class-6/starter-files/6-1-while-loop-sum.php`.
2. Complete the TODO to calculate `$sum`.
3. Run the script and confirm the output matches the expected output in the
  file.

## Exercise 6-2: `do...while` Loop

In this exercise, you will use a `do...while` loop and observe that it runs
at least once.

1. Open `class-6/starter-files/6-2-do-while-loop.php`.
2. Complete the TODO using `do...while`.
3. Run the script and confirm the output matches the expected output in the
  file.

## Exercise 6-3: `break` and `continue`

In this exercise, you will use `continue` to skip iterations and `break` to
stop a loop early.

1. Open `class-6/starter-files/6-3-break-and-continue.php`.
2. Complete the TODOs to update `$count` and `$sum`.
3. Run the script and confirm the output matches the expected output in the
  file.

## Exercise 6-4: `for` Loop + Building a List String

In this exercise, you will build a comma-separated list using a `for` loop,
without a trailing comma.

1. Open `class-6/starter-files/6-4-for-loop-list.php`.
2. Complete the TODO to build `$list`.
3. Run the script and confirm the output matches the expected output in the
  file.

## Exercise 6-5: Alternative Loop Syntax + Guardrail

In this exercise, you will update your Madlibs script from class 5 to use a loop
to allow the user to play multiple times.

1. Open `class-6/starter-files/6-5-madlibs.php` and paste in your Madlibs script from class 5.
2. Open `class-6/starter-files/6-5-madlibs-functions.php` and paste in your Madlibs functions from class 5.
3. Add a loop to allow the user to play multiple times, using a prompt of "Would you like to go again? (y/n)" and looping if the user enters "y" or "Y" and ending in all other cases.
4. Run the script and confirm it works as expected, allowing you to play multiple times and ending when you choose not to play again.
